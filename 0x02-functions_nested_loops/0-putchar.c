#include <stdio.h>
/**
 * main - write a program that prints _putchar, followed by a new line
 *
 * return: Always 0 (success)
 */
int main(void)
{
	char ch = '_putchar';

	putchar(ch);
	putchar('\n');

	return (0);
}
