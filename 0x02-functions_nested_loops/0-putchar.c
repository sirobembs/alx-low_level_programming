#include <main.h>
/**
 * main - writes a program that prints _putcar, followed by a new line
 *
 * return: always 0 (success)
 */
int main(void)
{
	char ch = '_putchar';

	putchar(ch);
	putchar('\n');

	return (0);
}
		
